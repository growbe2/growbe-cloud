import {GrowbeMainboardImageConfig} from "../../models";
import {GrowbeImangeConfigService} from "../../services";
import {boardId} from "../fixtures/data";

/*
describe.only('Growbe Image Config', () => {

  let service: GrowbeImangeConfigService;

  before('setupService', async function () {
    service = new GrowbeImangeConfigService();
  });

  after(async () => {
    
  });

  it('Render all template file for a growbe', () => {
    const folder = service.generateConfigFiles(boardId, new GrowbeMainboardImageConfig({
        environment: "dev",
        wifi: {
            ssid: "BerlingoQC",
            password: "WaR@aftt",
            country: "CA"
        },
        ssh: {
            enable: true,
            remoteAddr: "root@assets.inner.wquintal.ca"
        },
        fluentLog: {
            enable: true,
        }
    }));
    service.generateArchiveFromConfigFiles(boardId, folder);
  }).timeout(10000);
});

*/
 
