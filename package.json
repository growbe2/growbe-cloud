{
  "name": "@growbe2/cloud",
  "version": "1.1.3",
  "description": "Mono-repo pour growbe-cloud project",
  "repository": {
    "type": "git",
    "url": "https://github.com/growbe2/growbe-cloud/"
  },
  "scripts": {
    "version": "node -p \"require('./package.json').version\"",
    "setversion": "export VERSION=$(npm run version --silent) && cd proto && npm --no-git-tag-version version $VERSION && cd ../growbe-proxy && npm --no-git-tag-version version $VERSION && cd ../growbe-portal && npm --no-git-tag-version version $VERSION",
    "get-env": "scp -r root@api.growbe.wquintal.ca:envs docker/"
  }
}
